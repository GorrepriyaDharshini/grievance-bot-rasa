# --- Web stack (Python 3.8+) — install in every environment

psycopg2-binary
requests
flask
flask-cors>=4.0,<5.0
# --- RASA (Python 3.8–3.10 ONLY; not installable on Python 3.11+ with OSS 3.6.x)
# Create a separate venv with Python 3.10, then:
#   pip install "rasa>=3.6.0,<3.7.0"
# Or use:
#   pip install -r requirements-rasa.txt
# inside that interpreter.
